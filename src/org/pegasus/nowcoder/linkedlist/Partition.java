package org.pegasus.nowcoder.linkedlist;

public class Partition {
    public ListNode partition (ListNode head, int x) {
        // write code here
        ListNode bigHead=null,bigTail=null,midHead=null,midTail=null,smallHead=null,smallTail=null;
        while(head!=null){
           if(head.val> x){

           }else if(head.val==x){

           }else{

           }
        }
        return  bigHead;
    }
}
